# TODO List

## Settings.py
- [x] Set the french as the default language

## Home Page /
- [x] for the `.serviceChoices` and `.quote` Set the `height:460px` with an overflow
- For `.grand` and `.nextBtn` in `#composer` section change the behaviour so:
    - [x] they are fixed to the top right under the navbar 
    - [x] they are hidden by default 
    - [x] they get shown only when step two of the composer is on the viewport 
    - [x] use minimalist and responsive design 
    - [x] hide them from the `aside.quote` 
- [x] set the `border-radius:12px` for `#composerFloatingPriceBar`
- [x] in `.serviceChoice` restyle the `isvc-check-icon.fas.fa-check` so you center the icon 
- [x] fix the following issue:
    - when clicking `.serviceChice` the user must continue navigation instead of going to the first items of the list
- [x] Set the `position:fixed` of the `.gallerySpacesNav`, fix it to the top
- [x] hide the `.projectCostInputBox`  

## Space Details Page /dashboard/design/spaces/<id>
- [x] Correct the `z-index` issue of the `custom_select`

## Service & Catalog options page /dashboard/design/packages
- [x] Correct this issue `packages/:1811 Uncaught SyntaxError: Identifier 'tabFrBtn' has already been declared` 
- [x] Move the filter and search options to a modal
- [x] Redesign the Tabs in new service modal and fix the contrast issue if :hover
- [x] Change the layout of Multilanguage content fields 
- [x] Use `custom_select` in the service create/edit modal
- [x] Use `custom_select` in the filter modal
- [x] fix the `z-index` of the `custom_select` in the modal

