this is a todo list you need to implement, each time a task is finished update this list to mark the task as finished

## Site Settings

- [x] edit the ASSERT_VERSION so you always get the latest static files both in production and dev servers

## Home Page /

- [x] Make the `.space-card-img img` as the background image and move the text and checkbox above of it
- [x] Make the .space-card a square
- [x] remove the checkbox from .space-card and replace it by a checkmark when the space is selected - the checkmark must be in the center -
- [x] in `.space-card` replace the price by title and centre the price horizontally and vertically
- [x] in `.space-card` keep the title at the bottom left 
- [x] remove the text from .project-rect-card and leave only the image
- [x] remove the tagline from the `recent projects` section
- [x] centre the `recent projects` title 
- [x] make the `#floatingBar` responsive

## Portfolio item Creation/edit Page /dashboard/portfolio/edit
- [x] Make all the model fields optional except the Thumbnail

## Portfolio Page

- [x] restyle the `.portfolio-card` so it matches exactly the `.space-card` in home page `/`
- [x] remove the `view project` button and the meta data from the `.portfolio-card`
- [x] add the `360 deg` button to the top right of the card  
- [x] make the `360 deg` button a text + icon and with border radius of 12px

## Portfolio item Details Page /portfolio/103/

- [x] remove the gallery button `#galleryToggle` entirely
- [x] the user must click any area at the bottom of the screen to view the gallery items -no need for a button- 
- [x] Center the 360 button in the bottom and make it wider 
- [x] remove the `.viewer-counter` entirely
- [x] hide the back button for mobile screens
- [x] make the images fullscreen even in mobile screens
